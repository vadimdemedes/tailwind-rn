const tailwindTest = require('../helpers/tailwind-test');

tailwindTest('border-solid', {
	borderStyle: 'solid'
});

tailwindTest('border-dashed', {
	borderStyle: 'dashed'
});

tailwindTest('border-dotted', {
	borderStyle: 'dotted'
});
